::xowiki::Page new \
	-set name en:reference_kv_module.page \
	-title "Module Command Reference" \
	-text {
<p><u>Name:</u></p>
<p>module - Create a module with a title and structure</p>

<p><u>Synopsis:</u></p>
<p><b>module</b> <i>key value</i></p>

<p><u>Description:</u></p>
<p>A module has a <b>title</b> and <b>structure</b> containing a comma separated list of element types, referenced via their <i>identifier</i> (e.g. (textpageIntro, question1, v1 etc.)). Note that no <i>identifier</i> is needed for the module itself.</p>

<p>Valid keys are:</p>

<p><b>module</b> <b>title</b> title_of_module<br>
<b>module</b> <b>structure</b> (?element1, element2, ... elementN?)<br>

<p><u>Example:</u></p>
<code class="example">module title "Tutorial Learning Module" <br>module structure (video, tutorialTextPage, q1)</code>
} \
	-description {Module}
