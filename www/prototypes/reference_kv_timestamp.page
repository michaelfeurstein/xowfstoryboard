::xowiki::Page new \
	-set name en:reference_kv_timestamp.page \
	-title "Timestamp Command Reference" \
	-text {
<p><u>Name:</u></p>
<p>timestamp - Create a timestamp for a video</p>

<p><u>Synopsis:</u></p>
<p><b>timestamp</b> <i>identifier</i> key <i>value</i></p>

<p><u>Description:</u></p>
<p>Creates a timestamp element based on key-value pairs. The provided <i>identifier</i> is used in order to reference the element. The <b>title</b> is used to display a clickable link. The <b>time</b> defines the start of the video at a certain time in seconds.</p>

<p>Valid keys are:</p>

<p><b>timestamp</b> <i>identifier</i> <b>title</b> string inside quotes<br>
<b>timestamp</b> <i>identifier</i> <b>time</b> time in seconds</p>

<p><u>Example:</u></p>
<code class="example">timestamp t01 title "Tutorial Timestamp" <br>timestamp t01 time 63</code>
} \
	-description {Timestamp}
