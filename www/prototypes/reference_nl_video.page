::xowiki::Page new \
	-set name en:reference_nl_video.page \
	-title "Element Type: Video" \
	-text {
<p><u>Name:</u></p>
<p>video</p>

<p><u>Synopsis</u></p>
<p>The element type video has the following attributes:</p>
<p><i>ATTRIBUTE</i>: <b>id</b> <i>value</i>: any characters without spaces
<br/><i>ATTRIBUTE</i>: <b>URL</b> <i>value</i>: embeddable youtube link</p>
<p>Element types can be added with command <b>Add</b><p>
<p><i>ELEMENT_TYPE</i>: <b>timestamps</b> <i>value</i>: comma separated list of timestamp identifiers in brackets</p>

<p><u>Description:</u></p>
<p>A video has an <b>id</b> in order to reference it. An <b>id</b> can hold upper and lower case characters without any spaces. The <b>URL</b> links to an embeddable video link. Currently only youtube links are supported. Additionally one can add a list of comma separated timestamp ids in brackets in order to add <b>timestamps</b> to a video.</p>

<p><u>Example:</u></p>
<code class="example">Create video with id tutorialVideo<br>Set URL of tutorialVideo to https://www.youtube.com/embed/0siisFJUKh4<br>Add timestamps (ts01, ts02) to tutorialVideo</code>
} \
	-description {Video}
