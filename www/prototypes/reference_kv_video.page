::xowiki::Page new \
	-set name en:reference_kv_video.page \
	-title "Video Command Reference (KV)" \
	-text {
<p><u>Name:</u></p>
<p>video - Create a video element</p>

<p><u>Synopsis:</u></p>
<p><b>video</b><i>?identifier? key value</i></p>

<p><u>Description:</u></p>
<p>Creates a video element based on key-value pairs. The provided <i>identifier</i> is used together with the command name as an id for the element in order to reference it (e.g.: video1, videoSession01). If no <i>identifier</i> is provided, future elements will be overwritten. The URL needs to be an embeddable URL. Timestamps are added as comma separated lists in brackets (e.g. (timestamp01, timestamp02, timestamp03, timestamp04))</p>

<p> Valid keys are:</p>

<p><b>video</b><i>?identifier?</i> <b>URL</b> embeddable_url_of_video<br>
<b>video</b><i>?identifier?</i> <b>timestamp</b> (timestamp_id ?, timestamp_id?)</p>

<p><u>Video Example:</u></p>
<p>videoSession01 URL https://www.youtube.com/embed/0siisFJUKh4<br>videoSession01 timestamp (intro, definition, topic1, topic2, end)</p>
} \
	-description {Video}
