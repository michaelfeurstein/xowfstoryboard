::xowiki::Page new \
	-set name en:reference_kv_video.page \
	-title "Video Command Reference" \
	-text {
<p><u>Name:</u></p>
<p>video - Create a video element</p>

<p><u>Synopsis:</u></p>
<p><b>video</b> <i>identifier</i> key <i>value</i></p>

<p><u>Description:</u></p>
<p>Creates a video element based on key-value pairs. The provided <i>identifier</i> is used in order to reference the element. A video element only needs a <b>URL</b> to work. The <b>URL</b> needs to be an embeddable video link. Currently only youtube links are supported. A video can contain <b>timestamps</b> which are added as comma separated lists in brackets (e.g. (ts1, ts2, ts3, t4))</p>

<p> Valid keys are:</p>

<p><b>video</b> <i>identifier</i> <b>URL</b> embeddable_youtube_link<br>
<b>video</b> <i>identifier</i> <b>timestamp</b> (?timestamp_id1, timestamp_id2, ... timestamp_idN?)</p>

<p><u>Example:</u></p>
<code class="example">video tutorialVideo URL https://www.youtube.com/embed/gNYbvsruKHs<br>video tutorialVideo timestamp (ts01, ts02)</code>

<p id="inserter" onclick="myFunction()">Insert</p>

<script>
function myFunction() {
	document.getElementById("inserter").innerHTML = "WORKS";
}
</script>
} \
	-description {Video}
